#include "easyfind.hpp"



int main( void ) {
	try {
		;
	}
	catch (std::exception& e) {
		std::cout<< "Element not found !" <<std::endl;
	}
	try {
		;
	}
	catch (std::exception& e) {
		std::cout<< "Element not found !" <<std::endl;
	}
	try {
		;
	}
	catch (std::exception& e) {
		std::cout<< "Element not found !" <<std::endl;
	}
	return (0);
}